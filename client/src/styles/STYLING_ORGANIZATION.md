# Організація файлів стилів - Пояснення

## Питання: Де розміщувати файли стилів?

### Є два підходи:

## 📁 Підхід 1: Один файл стилів для кожного компонента

```
pages/
└── auth/
    ├── LoginPage.jsx
    ├── LoginPage.scss      # Стилі тільки для LoginPage
    ├── RegisterPage.jsx
    └── RegisterPage.scss   # Стилі тільки для RegisterPage
```

**Імпорт:**
```jsx
// LoginPage.jsx
import './LoginPage.scss';
```

**Переваги:**
- ✅ Чітке розділення - один компонент = один файл стилів
- ✅ Легко знайти стилі конкретного компонента
- ✅ При видаленні компонента видаляєте і його стилі

**Недоліки:**
- ❌ Дублювання коду якщо компоненти схожі
- ❌ Важче підтримувати консистентність

**Коли використовувати:**
- Коли компоненти мають різний дизайн
- Коли компоненти можуть змінюватись незалежно

---

## 📁 Підхід 2: Спільний файл стилів для схожих компонентів (використовуємо ми)

```
pages/
└── auth/
    ├── LoginPage.jsx
    ├── RegisterPage.jsx
    └── auth.scss           # Спільні стилі для обох
```

**Імпорт:**
```jsx
// LoginPage.jsx
import './auth.scss';

// RegisterPage.jsx
import './auth.scss';
```

**Переваги:**
- ✅ Немає дублювання коду
- ✅ Легко підтримувати консистентність дизайну
- ✅ Зміни в одному файлі → оновлення обох сторінок
- ✅ Менше файлів для керування

**Недоліки:**
- ❌ Якщо потрібно змінити тільки одну сторінку - може бути складніше

**Коли використовувати:**
- Коли компоненти мають схожу структуру (наш випадок - форми авторизації)
- Коли важлива консистентність дизайну
- Коли компоненти логічно пов'язані

---

## Наш вибір: Підхід 2

### Чому один файл `auth.scss` для LoginPage і RegisterPage?

1. **Схожа структура форм:**
   - Обидві сторінки мають однакові елементи: заголовок, форма, поля вводу, кнопка submit
   - Різниця тільки в кількості полів та тексті

2. **Консистентність дизайну:**
   - Користувач має бачити однаковий дизайн на обох сторінках
   - Якщо змінимо відступи в LoginPage - треба змінити і в RegisterPage

3. **Менше коду:**
   - Замість 2 файлів по 100 рядків → 1 файл на 100 рядків
   - Легше підтримувати

### Структура файлу `auth.scss`:

```scss
// Загальні стилі для auth-page
.auth-page { }

// Контейнер форми (використовується в обох)
.auth-container { }

// Заголовок (використовується в обох)
.auth-header { }

// Форма (використовується в обох)
.auth-form { }

// Якщо потрібні специфічні стилі:
.login-page {
  // Стилі тільки для login
}

.register-page {
  // Стилі тільки для register
}
```

---

## Коли створювати окремі файли?

### Приклад: Сторінки Products

```
pages/
└── products/
    ├── ProductsListPage.jsx
    ├── ProductsListPage.scss    # Окремий файл
    ├── ProductDetailPage.jsx
    └── ProductDetailPage.scss   # Окремий файл
```

**Чому окремо?**
- Різна структура: список сітка vs одна велика картка
- Різні компоненти: фільтри vs галерея зображень
- Можуть змінюватись незалежно

---

## Альтернативний підхід: Shared стилі

Ще один варіант - створити папку `shared`:

```
pages/
└── auth/
    ├── LoginPage.jsx
    ├── RegisterPage.jsx
    └── shared/
        └── auth.scss
```

Або навіть:

```
styles/
├── _variables.scss
├── global.scss
└── shared/
    └── auth.scss          # Можна використовувати в різних місцях
```

**Коли використовувати:**
- Якщо стилі можуть використовуватись не тільки в auth сторінках
- Якщо хочете більш чітко показати що це спільний ресурс

---

## Підсумок

**Наша структура:**
```
client/src/
├── styles/
│   ├── _variables.scss      # Глобальні змінні
│   └── global.scss           # Глобальні стилі
├── components/
│   ├── Layout.jsx
│   └── Layout.scss           # Стилі Layout (1 компонент = 1 файл)
└── pages/
    └── auth/
        ├── LoginPage.jsx
        ├── RegisterPage.jsx
        └── auth.scss         # Спільні стилі (логічно пов'язані)
```

**Правило:**
- **Один файл для кожного компонента** - якщо компоненти різні
- **Спільний файл** - якщо компоненти схожі та логічно пов'язані

---

## Практичні приклади

### ✅ Добре: Спільний файл для форм
```
auth/
├── LoginPage.jsx
├── RegisterPage.jsx
└── auth.scss              # Обидві форми схожі
```

### ✅ Добре: Окремі файли для різних компонентів
```
products/
├── ProductCard.jsx
├── ProductCard.scss       # Картка товару
├── ProductGrid.jsx
└── ProductGrid.scss       # Сітка - інша структура
```

### ❌ Погано: Дублювання коду
```
auth/
├── LoginPage.jsx
├── LoginPage.scss         # 100 рядків
├── RegisterPage.jsx
└── RegisterPage.scss      # Ті самі 100 рядків скопійовано
```

---

## Важливо!

Незалежно від підходу, **завжди імпортуйте стилі в компонент**:

```jsx
import './auth.scss';  // або './LoginPage.scss'

function LoginPage() {
  // ...
}
```

Не імпортуйте стилі в `main.jsx` або `App.jsx` (крім `global.scss`)!

---

**Успіхів у організації коду! 🎨**
